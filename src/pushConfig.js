// Push notification config for browser-based notifications (no extra app,
// no paid service — this uses the free, built-in Web Push standard).
//
// This key pair was generated for you already — you don't need to do
// anything else here. The matching private key lives as an environment
// variable on Vercel (never in this file, since this file ships to
// everyone's browser).
//
// If you ever need to regenerate the keys (e.g. you suspect the private
// key leaked), generate a fresh pair and update both this file AND the
// VAPID_PRIVATE_KEY environment variable in Vercel to match.
export const VAPID_PUBLIC_KEY = "BPnrwdxTZgKrPat761KN9HYBBF_7Sl96WCl1lHGNH588eDH43p0DkR35rRnNZXrmVhZ6JJqUzL8AWNgfqLT2Y-w";
